/**
 * Una promesa es un objeto que representa la terminación o el 
 * fracaso eventual de una operación asíncrona
 */


const promise = new Promise(memoizedCallback = useCallback(
    () => {
        callback
    },
    [input],
))